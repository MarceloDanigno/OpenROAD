
std::vector<int> louvain_partclusmanager(std::vector<unsigned long long> degrees,
                                         std::vector<int> links,
                                         std::vector<long double> weights,
                                         std::vector<int> nodes_w,
                                         int returnLevel);
